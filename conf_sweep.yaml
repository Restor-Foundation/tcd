program: vanilla_model.py
method: random
metric:
  name: loss #val_loss
  goal: minimize
parameters:
  learning_rate:
    min: 1e-5
    max: 1e-1
  optimizer:
    values: ["adam"]
loss: ["focal", "ce"] #weighted ce loss
segmentation_model: [unet, deeplabv3+]
backbone: [resnet18, resnet34, resnet50]